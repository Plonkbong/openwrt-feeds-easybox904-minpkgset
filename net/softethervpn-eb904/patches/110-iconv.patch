diff --git a/src/Mayaqua/Mayaqua.h b/src/Mayaqua/Mayaqua.h
index 1a191e8..45b22e3 100644
--- a/src/Mayaqua/Mayaqua.h
+++ b/src/Mayaqua/Mayaqua.h
@@ -289,6 +289,7 @@ int PASCAL WinMain(HINSTANCE hInst, HINSTANCE hPrev, char *CmdLine, int CmdShow)
 #include <ifaddrs.h>
 #endif	// MAYAQUA_SUPPORTS_GETIFADDRS
 
+/*
 #ifdef	UNIX_LINUX
 typedef void *iconv_t;
 iconv_t iconv_open (__const char *__tocode, __const char *__fromcode);
@@ -298,8 +299,11 @@ size_t iconv (iconv_t __cd, char **__restrict __inbuf,
                      size_t *__restrict __outbytesleft);
 int iconv_close (iconv_t __cd);
 #else	// UNIX_LINUX
+*/
 #include <iconv.h>
+/*
 #endif	// UNIX_LINUX
+*/
 
 
 
